{
  "name": "demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "refresh:dev": "cd ../library && npm run build-dev && npm link && cd ../ts-demo && npm link hazeline && npm run build-dev",
    "refresh:prod": "cd ../library && npm run build-prod && npm link && cd ../ts-demo && npm link hazeline && npm run build-prod",
    "build-dev": "rimraf dist && webpack --mode development ./src/index.ts --output ./dist/bundle.js --watch",
    "build-prod": "rimraf dist && webpack --mode production ./src/index.ts --output ./dist/bundle.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hazeline": "file:../library/hazeline-0.0.0.tgz"
  },
  "devDependencies": {
    "rimraf": "2.6.2",
    "ts-loader": "4.4.0",
    "typescript": "2.9.1",
    "webpack": "4.12.0",
    "webpack-cli": "3.0.3",
    "webpack-dev-server": "3.1.4"
  }
}